export interface Task{
    id: string,
    turno: string,
    planta: string, 
    linea: string,
    tiempo: string, 
    piezaBuena: string, 
    piezaPlan: string, 
    piezaScrap: string, 
    cabezas: string, 
    tiempoMuerto: string, 
}